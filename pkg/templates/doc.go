// Package templates assembles and validates IMA template entries.
//
// It combines shared base fields with template-specific extra fields and
// verifies entry integrity by recomputing the template hash.
package templates
